# TaskFlow Sentinel — roadmap

- [x] Frontend UI (Claymorphism) — all pages
- [ ] Python FastAPI backend (models, services, scheduler, fault tolerance, websocket, API)
- [ ] Alembic migrations + seed script + demo worker runtime
- [ ] Docker Compose (frontend, backend, postgres, redis)
- [ ] pytest suite incl. worker-failure → recovery integration test
- [ ] Frontend: real API + WebSocket integration (VITE_API_URL / VITE_WS_URL)
- [ ] Remove the static frontend simulation entirely (backend is the only source of truth)
- [ ] PWA: manifest + icons so the app can be installed on a system
